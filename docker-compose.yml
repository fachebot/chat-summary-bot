version: '3.8'

services:
  bot:
    build: .
    ports:
      - "${WEB_PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
      - ./etc/config.yaml:/app/etc/config.yaml
    env_file:
      - .env
    environment:
      - TZ=UTC
    restart: unless-stopped
